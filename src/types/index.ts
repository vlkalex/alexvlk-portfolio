export type { Project, ProjectLink } from "./project";

export interface Screenshot {
  src: string;
  alt: string;
}

export interface Stat {
  value: string;
  label: string;
}

export interface ApproachItemContent {
  title: string;
  description: string;
}
